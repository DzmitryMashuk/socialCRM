{% extends 'base.html.twig' %}

{% block title %}Работник #{{ user.id }}{% endblock %}

{% block body %}
    <h1>Работник #{{ user.id }}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user.id }}</td>
            </tr>
            <tr>
                <th>Почта</th>
                <td>{{ user.email }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('user') }}">Назад</a>

    <a href="{{ path('user_edit', {'id': user.id}) }}">Редактировать</a>

    {{ include('user/_delete_form.html.twig') }}
{% endblock %}
