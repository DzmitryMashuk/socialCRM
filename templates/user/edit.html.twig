{% extends 'base.html.twig' %}

{% block title %}Редактирование работника #{{ user.id }}{% endblock %}

{% block body %}
    <h1 class="mb-4">Редактирование работника</h1>

    <a href="{{ refererUrl|default(path('user')) }}" class="btn btn-primary mb-3">Назад</a>

    {{ include('user/_form.html.twig', {'button_label': 'Сохранить'}) }}

    <a href="{{ refererUrl|default(path('user')) }}" class="btn btn-primary mb-3">Назад</a>
{% endblock %}
