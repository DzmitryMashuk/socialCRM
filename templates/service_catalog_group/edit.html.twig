{% extends 'base.html.twig' %}

{% block title %}Редактирование группы услуги #{{ serviceCatalogGroup.id }}{% endblock %}

{% block body %}
    <h1 class="mb-4">Редактирование группы услуги #{{ serviceCatalogGroup.id }}</h1>

    <a href="{{ refererUrl|default(path('service_catalog_group')) }}" class="btn btn-primary mb-3">Назад</a>

    {{ include('service_catalog_group/_form.html.twig', {'button_label': 'Сохранить'}) }}

    <a href="{{ refererUrl|default(path('service_catalog_group')) }}" class="btn btn-primary mb-3">Назад</a>

{% endblock %}
