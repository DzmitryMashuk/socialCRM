{% extends 'base.html.twig' %}

{% block title %}Редактировать услугу {{ service.id }}{% endblock %}

{% block body %}
    <h1 class="mt-5 mb-4">Редактировать услугу {{ service.id }}</h1>

    <a href="{{ path('service') }}" class="btn btn-primary">Назад</a>

    {{ include('service/_form.html.twig', {'button_label': 'Сохранить'}) }}

    <a href="{{ path('service') }}" class="btn btn-primary">Назад</a>
{% endblock %}
