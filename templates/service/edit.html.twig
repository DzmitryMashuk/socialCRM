{% extends 'base.html.twig' %}

{% block title %}Редактировать услугу {{ service.id }}{% endblock %}

{% block body %}
    <h1>Редактировать услугу {{ service.id }}</h1>

    {{ include('service/_form.html.twig', {'button_label': 'Сохранить'}) }}

    <a href="{{ path('service') }}">Назад</a>

    {{ include('service/_delete_form.html.twig') }}
{% endblock %}
