{% extends 'base.html.twig' %}

{% block title %}Создание услуги{% endblock %}

{% block body %}
    <h1 class="mt-5 mb-4">Создание услуги</h1>

    <a href="{{ refererUrl|default(path('service_catalog')) }}" class="btn btn-primary mb-3">Назад</a>

    {{ include('service_catalog/_form.html.twig') }}

    <a href="{{ refererUrl|default(path('service_catalog')) }}" class="btn btn-primary mb-3">Назад</a>
{% endblock %}
