{% extends 'base.html.twig' %}

{% block title %}Редактирование клиента #{{ client.id }}{% endblock %}

{% block body %}
    <h1>Редактирование клиента #{{ client.id }}</h1>

    <a href="{{ path('client') }}" class="btn btn-primary">Назад</a>

    {{ include('client/_form.html.twig', {'button_label': 'Сохранить'}) }}

    <a href="{{ path('client') }}" class="btn btn-primary">Назад</a>

{% endblock %}
