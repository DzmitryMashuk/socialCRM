{% extends 'base.html.twig' %}

{% block title %}Client{% endblock %}

{% block body %}
    <h1>Client</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ client.id }}</td>
            </tr>
            <tr>
                <th>First_name</th>
                <td>{{ client.firstName }}</td>
            </tr>
            <tr>
                <th>Middle_name</th>
                <td>{{ client.middleName }}</td>
            </tr>
            <tr>
                <th>Last_name</th>
                <td>{{ client.lastName }}</td>
            </tr>
            <tr>
                <th>Age</th>
                <td>{{ client.age }}</td>
            </tr>
            <tr>
                <th>Phone</th>
                <td>{{ client.phone }}</td>
            </tr>
            <tr>
                <th>Guardian_name</th>
                <td>{{ client.guardianName }}</td>
            </tr>
            <tr>
                <th>Guardian_phone</th>
                <td>{{ client.guardianPhone }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('client') }}">back to list</a>

    <a href="{{ path('client_edit', {'id': client.id}) }}">edit</a>

    {{ include('client/_delete_form.html.twig') }}
{% endblock %}
